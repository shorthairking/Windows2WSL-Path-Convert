# 任务名称：WSL路径转换（暂定）

## 任务背景描述

在日常使用WSL时，在终端或者其他场景下，经常会出现需要访问windows路径的情况，而WSL使用的是Linux风格的路径，这就需要进行路径转换。为了提高工作效率，减少手动转换的麻烦，计划开发一个工具来实现WSL路径与Windows路径之间的自动转换。

## 任务目标

1. 开发一个工具或者服务类型的应用，安装于WSL环境中（指定发行版为ubuntu），能够实现WSL路径与Windows路径之间的自动转换。

2. 应用的具体类型根据任务的具体需求来确定，可以是命令行工具、桌面应用或者其他形式。

3. 预期实现的效果如下：

- 用户在发行版中安装该工具并设置好后，当发行版启动时自动启动并进入服务状态
- 用户在各种情况下（包括但不限于在调用命令或者使用脚本传入路径参数时）输入windows路径，工具能够自动将其转换为WSL路径，替换原有的路径参数并执行
- 用户在日常使用且在通常情况（不报错）中无感知地使用该工具。

预想的效果如下：

假设已经通过脚本或者其他方式完成了工具的安装和配置，用户在使用WSL时输入如下命令：

```bash
some_command C:\Users\username\Documents\file.txt
```

在具体执行时，该指令被静默转换为：

```bash
some_command /mnt/c/Users/username/Documents/file.txt #实际执行结果
```

该过程中没有工具的显式调用或提示（除非检测到错误或者其他情况），用户无需手动进行路径转换。

## 任务过程中的已知要求或限制

1. 当前开发环境为WSL Ubuntu 24.04 LTS。
2. 在开发过程中，如需调用外部依赖或者工具请注明于.agent或者其他提供给智能体相关的配置文件中。
3. 在开发过程中如果需要设置TODO或者其他用于记载开发过程的清单（类似于todo list），请每个TODO项目做成一个完整的skill，以减少prompt的长度和复杂度。
4. 在完成每个步骤后，请提交一次git commit，并在commit message中注明完成的内容。
5. 该任务需要进行测试，但在当前阶段请不要进行测试，测试相关的内容请在后续阶段进行。